- if:
    condition:
      lambda: return x == id(btn_info);
    then:
    - lvgl.page.show: info_page
- if:
    condition:
      lambda: return x == id(btn_thermostat);
    then:
    - lvgl.page.show: therm_page
- if:
    condition:
      lambda: return x == id(btn_vacuum);
    then:
    - lvgl.page.show: vacuum_page
- if:
    condition:
      lambda: return x == id(btn_cover);
    then:
    - lvgl.page.show: cover_page
- if:
    condition:
      lambda: return x == id(btn_fan);
    then:
    - lvgl.page.show: fan_page
- if:
    condition:
      lambda: return x == id(btn_texto);
    then:
    - lvgl.page.show: texto_page
